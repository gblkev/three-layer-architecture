spring.main.banner-mode=off
# Defined dynamically during tests
server.port=-1

spring.redis.host=Defined dynamically
# Defined dynamically during tests
spring.redis.port=-1
spring.redis.database=1

sandbox.use-random-ports=false

sandbox.mysql.db-name=cars_db
# Defined dynamically during tests
sandbox.mysql.port=-1
sandbox.mysql.user=test_user
sandbox.mysql.password=test_password

# Defined dynamically during tests
sandbox.redis.port=-1
sandbox.redis.password=test_password
sandbox.redis.database=1
sandbox.redis.load-init-script=false

# Defined dynamically during tests
sandbox.rest.mockserver-port=-1
sandbox.rest.init-mocks=false

# Defined dynamically during tests
sandbox.kafka.port=-1
# Defined dynamically during tests
sandbox.zookeeper.port=-1
sandbox.kafka.topics=threelayerarchitecture.create-color